##URL语法

```
<scheme>://<user>/<password>@<host>:<port>/<path>;<params>?<query>#<frag>

方案 *: http, https, ftp
用户  :
密码  :
主机 *: 
端口 *: 
路径  : 服务器主机上的文件系统路径，或用于服务器程序对请求路由
参数  : 
查询  : ?offset=0&limit=10
片段  :
```


